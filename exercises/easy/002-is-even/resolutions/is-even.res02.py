# This is the implicit/concise solution to the "002 - Is Even" problem
def is_even(num):
    # Check if the number is divisible by 2.
    # Returns True if divisible (even), False if not (odd).
    return num % 2 == 0