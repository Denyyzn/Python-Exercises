# This is the solution to the previous problem
def double(num):
    # Multiply the number by 2 and return the result directly.
    # We could also store the result in a variable before returning it.
    return num * 2
